{
  "name": "Excalidraw",
  "prisma": {
    "schema": "packages/db/prisma/schema.prisma"
  },
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "migrate:dev": "pnpm prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "migrate:reset": "pnpm prisma migrate reset --schema packages/db/prisma/schema.prisma",
    "studio": "prisma studio schema"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.17.1"
  }
}
